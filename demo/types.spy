cfunc print(n: int) -> void;

func fib(n: int) -> int {
  if (n <= 1) {
    return 1;
  }
  return fib(n - 1) + fib(n - 2);
}

func main() -> void {

  x: int;
  y: int;
  
  for (x = 0; x < 4; x = x + 1) {
    for (y = 0; y < 4; y = y + 1) {
      print(x + y);
    }
  }

}

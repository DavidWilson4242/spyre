cfunc print(n: int) -> void;

func factorial(n: int) -> int {
  if (n <= 1) {
    return 1;
  }
  return n * factorial(n - 1);
}

func fib(n: int) -> int {
  if (n <= 1) {
    return 1;
  }
  return fib(n - 1) + fib(n - 2);
}

func main() -> void {
  
    print(factorial(5));
    print(fib(6));

}

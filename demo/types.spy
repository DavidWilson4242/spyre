Test: struct {
  foo: int;
};

Vector3: struct {
  x: int;
  bar: Test;
}

func factorial(n: int) -> int {
  if (n <= 1) {
    return 1;
  }
  return n * factorial(n - 1);
}

func main() -> void {
  
  x: Vector3;

  factorial(10);

}

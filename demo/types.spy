Vector3: struct {
  
  x: int;
  y: int;
  z: int;
  
  method Sum() -> int;

};

method Vector3.Sum() -> int {
  return self.x + self.y + self.z;
}

func main() -> void {
  
  v: Vector3;
  v = new Vector3;
  
  print(v.Sum());


}
